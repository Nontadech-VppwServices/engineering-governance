# Domain Business Decision Records

Use this directory for business decisions shared by multiple projects inside one domain/product family.

Use `templates/BDR.md` when creating a record.

Only records with `status: accepted` are authoritative.
