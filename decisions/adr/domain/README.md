# Domain Architecture Decision Records

Use this directory for architecture decisions shared by multiple projects inside one business/technical domain but not necessarily the entire organization.

Examples:

- ecommerce integration patterns
- D365 integration standards
- RPA platform standards

Use `templates/ADR.md` when creating a record.

Only records with `status: accepted` are authoritative.
